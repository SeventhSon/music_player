<UserControl x:Class="Music_Player.View.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             DataContext="{Binding Source={StaticResource Locator}, Path=Settings}"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="45*"/>
            <RowDefinition Height="45"/>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Left" Margin="10,10,0,0" TextWrapping="Wrap" VerticalAlignment="Top" FontFamily="Tw Cen MT" FontWeight="Bold" FontSize="20"><Run Language="pl-pl" Text="Scanned folders"/></TextBlock>
        <ListBox ItemsSource="{Binding Directories}" Grid.Row="1" BorderBrush="#FFC9C9C9" Background="#FFD1CCCC">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <CheckBox IsChecked="{Binding NoRemove}"/>
                        <TextBlock Text="{Binding Name}"/>
                        <TextBlock Text="{Binding Path}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Content="Add directory" Command="{Binding AddCommand}" Margin="10,10,10,10" HorizontalAlignment="Center"/>
            <Button Content="Save" Command="{Binding SaveCommand}" Margin="10,10,10,10" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
