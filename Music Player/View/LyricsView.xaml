<UserControl x:Class="Music_Player.View.LyricsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="127*"/>
        </Grid.RowDefinitions>
        <TextBlock HorizontalAlignment="Center" Margin="10" TextWrapping="Wrap" Text="{Binding Lyrics}" VerticalAlignment="Top" Grid.Row="1"/>
        <TextBlock HorizontalAlignment="Center" Margin="10" TextWrapping="Wrap" Text="{Binding Title}" VerticalAlignment="Top" FontFamily="Tw Cen MT Condensed Extra Bold" FontSize="22"/>

    </Grid>
</UserControl>
