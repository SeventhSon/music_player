<UserControl x:Class="Music_Player.View.LibraryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             DataContext="{Binding Source={StaticResource Locator}, Path=Library}"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="45"/>
            <RowDefinition Height="64*"/>
        </Grid.RowDefinitions>
        <ListBox Margin="0,1,0,0" Grid.Row="1" BorderBrush="#FF979797" Background="#FF979797"/>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
            <Button Content="Songs" Width="75" Margin="5,0"/>
            <Button Content="Albums" Width="75" Margin="5,0"/>
            <Button Content="Artists" Width="75" Margin="5,0"/>
            <Button Content="Genre" Width="75" Margin="5,0"/>
        </StackPanel>
    </Grid>
</UserControl>
